% Generated by roxygen2 (4.1.1): do not edit by hand
% Please edit documentation in R/makeRetangular.R
\encoding{UTF-8}
\name{makeRetangular}
\alias{makeRetangular}
\title{Make a data.frame Rectangular by fill in missing records}
\usage{
makeRetangular(x, by, fill = NA)
}
\arguments{
\item{x}{a data frame.}

\item{by}{a vector of at least 2 variables from the data frame. If missing all variables in the data frame will be used.}

\item{fill}{the value used to fill in, default is \code{fill = 0}.}
}
\value{
a data object of the same class as \code{x}.
}
\description{
\code{makeRetangular} produces a complete rectangularization table by adding observations with missing data so that all combinations (interactions) of the specified variables exist.
}
\examples{
data <- data.frame(sex=c("female","male","male"),
race = c("black","black","white"), y = c(.5,.4,.1), x = c(32,40,53))

makeRetangular(data, by=c(sex,race))

makeRetangular(data, by=c(sex,race), fill=0)
}

